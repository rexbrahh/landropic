# Simplified .envrc without Nix flake
# use flake  # Commented out - Nix flake takes too long to load

# Just set basic environment variables
export RUST_BACKTRACE=1
export RUST_LOG=landropic=debug
export CARGO_BUILD_JOBS=4
export RUSTC_WRAPPER=""

echo "✅ Landropic environment loaded (without Nix)"